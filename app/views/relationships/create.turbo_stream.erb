<%= turbo_stream.replace(dom_id(@user, :follow), partial: 'relationships/unfollow', locals: { user: @user }) %>
<%= turbo_stream.replace(dom_id(@user, :follower_count), partial: 'relationships/follower_list', locals: { user: @user }) %>