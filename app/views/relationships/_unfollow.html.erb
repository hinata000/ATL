<%= turbo_frame_tag dom_id(user, :follow_user) do %>
  <%= form_with(model: current_user.relationships.find_by(followed_id: user.id), html: { method: :delete }) do |f| %>
    <%= f.submit 'フォロー中', class: 'btn btn-neutral w-full'%>
  <% end %>
<% end %>